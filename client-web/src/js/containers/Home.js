import * as React from 'react';
import { withSiteData } from 'react-static';
export default withSiteData(() => (React.createElement("div", null,
    React.createElement("h1", { style: { textAlign: 'center' } }, "Welcome to"))));
